---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import IrbProcessPage from '../../../react-pages/governance/IrbProcessPage.jsx';

const basePath = import.meta.env.BASE_URL;
const locale = 'en';

---

<BaseLayout title="IRB Process - Research Portal" locale={locale}>
  <main class="max-w-7xl mx-auto px-4 md:px-8 py-10 min-h-[60vh]">
    <IrbProcessPage client:load basePath={basePath} locale={locale} />
  </main>
</BaseLayout>
