---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import TopicGuideHealthComorbidityPage from '../../../react-pages/evidence-insights/TopicGuideHealthComorbidityPage.jsx';

const basePath = import.meta.env.BASE_URL;
const locale = 'ar';

---

<BaseLayout title="[AR] Health, Comorbidity & Risk - Research Portal" locale={locale}>
  <main class="max-w-7xl mx-auto px-4 md:px-8 py-10 min-h-[60vh]">
    <TopicGuideHealthComorbidityPage client:load basePath={basePath} locale={locale} />
  </main>
</BaseLayout>
